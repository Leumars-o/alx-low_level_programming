#include "main.h"
#include <stdio.h>

/**
  * main - entry point
  *
  * Description: function that prints its name
  *
  * @argc: arument count
  * @argv: argument array
  *
  * Return: Always 0 - success
  */
int main(int argc __attribute__((unused)), char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);
	return (0);
}
