#include "main.h"

/**
  * _abs - entry point
  *
  * @r: int to check
  *
  * Description: function that computes the absolute valur of an integer
  *
  * Return: int
  */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (-r);
}
