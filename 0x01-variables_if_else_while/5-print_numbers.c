#include <stdio.h>
#include <stdlib.h>

/**
  *main - entry point
  *Description:  Program that prints the digits of a number base
  *Return: Always 0
  */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	putchar('\n');
	return (0);
}
